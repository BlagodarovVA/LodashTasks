import _ from 'lodash';

let firstUserId = _.uniqueId('user_');
let secondUserId = _.uniqueId('user_');
let thirdUserId = _.uniqueId('user_');
let forUserId = _.uniqueId('user_');
let fiveUserId = _.uniqueId('user_');
let sixUserId = _.uniqueId('user_');
let sevenUserId = _.uniqueId('user_');
let eigthUserId = _.uniqueId('user_');
let nineUserId = _.uniqueId('user_');
let thenUserId = _.uniqueId('user_');
let elevenUserId = _.uniqueId('user_');

console.log(
	firstUserId,
	secondUserId,
	thirdUserId,
	forUserId,
	fiveUserId,
	sixUserId,
	sevenUserId,
	eigthUserId,
	nineUserId,
	thenUserId,
	elevenUserId
);
